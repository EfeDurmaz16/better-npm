# docker/Dockerfile.slim — npm-only minimal image
FROM node:20-alpine
RUN apk add --no-cache git
WORKDIR /app
COPY package.json ./
COPY src/ src/
RUN npm install --production --ignore-scripts
ENTRYPOINT ["node", "src/index.js"]
CMD ["--help"]
